let marks = [92, 85, 82, 95, 89];
// Printing the array
console.log(marks);

let heroes = ["Ironman", "Hulk","Batman", "Shaktiman", "Antman", "Spiderman"];
// Iterate the array using for loop
for(let idx=0; idx<heroes.length; idx++){
    console.log("heroes[", idx, "]", heroes[idx]);
}

let items = ["Dairy Products", "Fruits", "Cloths", "Stationary", "Beuty Products"];
// Iterate the array using for-of loop
for(let item of items){
    console.log(item);
}
